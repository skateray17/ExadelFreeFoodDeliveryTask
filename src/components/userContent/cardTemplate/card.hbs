<div class="card shadow">
    <div class="header"></div>
    <div class="card-inside">
        <div class="card-content {{#if emptyOrder}} jc {{/if}} {{#if emptyMenu}} center-text {{/if}}">
            {{#if emptyMenu}}
                <div class="not-available-menu">Меню еще недоступно</div>
            {{/if}}

            {{#if emptyOrder}}
                {{#if active}}
                <div class="not-chosen-menu">
                    <img src="{{imageUrl}}" class="not-chosen-menu-image"/>
                    <p>Меню не выбрано</p>
                </div>
                    {{/if}}
            {{/if}}
        </div>
        <div class="free-space"></div>
        {{#if active}}
            <div class="card-footer">
                {{#if sumPrice}}
                    <div class="order-price">Сумма заказа: {{totalPrice}} р</div>
                {{/if}}
                {{#if showButton }}
                    <button class="edit-button">{{button}}</button>
                {{/if}}
            </div>
        {{/if}}
    </div>
</div>